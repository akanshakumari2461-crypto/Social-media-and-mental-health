# Copy this file to .env and fill in values if needed.
# This project does NOT use any external API keys.
# No secrets are required to run locally or on Streamlit Cloud.

# Example (unused):
# OPENAI_API_KEY=your_key_here
